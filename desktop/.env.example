# Desktop (Electron main process) environment — copy to .env and fill in.
# The OpenAI key is used ONLY in the main process (server-side). Never expose it to the renderer.
# NOTE: a key was historically committed to this repo — rotate it at the OpenAI dashboard.
OPENAI_API_KEY=sk-your-key-here
